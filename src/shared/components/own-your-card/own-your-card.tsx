import { cardContainer, cardEntry } from "./own-your-card.css";

const OwnYourCard = () => {
	return (
		<section className={cardContainer}>
			<section className={cardEntry}>1</section>
		</section>
	);
};

export default OwnYourCard;
